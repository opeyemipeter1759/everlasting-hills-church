-- A section offering two or more ways to respond side by side (testimony and
-- salvation, say). TESTIMONY stays for the single-action case.
ALTER TYPE "EventSectionType" ADD VALUE IF NOT EXISTS 'RESPONSE';
