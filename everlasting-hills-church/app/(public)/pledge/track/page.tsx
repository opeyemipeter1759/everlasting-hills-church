import PledgeTrackingLookup from "@/components/pledge/PledgeTrackingLookup";

export const metadata = {
  title: "Find my pledge — Everlasting Hills Church",
  description: "Get the private tracking link for a pledge made without an account.",
};

export default function PledgeTrackingLookupPage() {
  return <PledgeTrackingLookup />;
}
