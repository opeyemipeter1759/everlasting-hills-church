import BooksClient from "@/components/dashboard/admin/books/BooksClient";

export const metadata = { title: "Books — Dashboard" };

export default function AdminBooksPage() {
  return <BooksClient />;
}
