import GatheringsClient from "@/components/dashboard/admin/gatherings/GatheringsClient";

export const metadata = {
  title: "Gatherings — Everlasting Hills Church",
};

export default function GatheringsPage() {
  return <GatheringsClient />;
}
