import EmailsClient from "@/components/dashboard/admin/emails/EmailsClient";

export const metadata = {
  title: "Emails — Everlasting Hills Church",
};

export default function EmailsPage() {
  return <EmailsClient />;
}
