import InventoryClient from "@/components/dashboard/admin/InventoryClient";

export const metadata = { title: "Inventory — Dashboard" };

export default function InventoryPage() {
  return <InventoryClient />;
}
