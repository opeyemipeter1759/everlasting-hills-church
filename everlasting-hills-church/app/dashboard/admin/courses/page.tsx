import CoursesAdminClient from "@/components/dashboard/admin/courses/CoursesAdminClient";

export const metadata = { title: "Manage Courses — Dashboard" };

export default function AdminCoursesPage() {
  return <CoursesAdminClient />;
}
