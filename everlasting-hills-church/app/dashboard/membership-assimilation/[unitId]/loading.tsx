import { BoardSkeleton } from "@/components/dashboard/follow-up/BoardSkeleton";

/** Shown while the Follow Up or Integration Team page is on its way. */
export default function Loading() {
  return <BoardSkeleton />;
}
