import PlanChooser from "@/components/dashboard/member/reading-plan/PlanChooser";

export const metadata = { title: "Choose a reading plan — Dashboard" };

export default function ReadingPlansPage() {
  return <PlanChooser />;
}
