# ── Resend (transactional email) ─────────────────────────────────────────────
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# The email address that receives contact form submissions
CONTACT_EMAIL=hello@everlastinghills.org

# The "from" address used in outbound emails.
# Must be a verified domain in Resend. During development you can use:
#   onboarding@resend.dev  (Resend's sandbox — works without domain verification)
# For production, set up your domain at https://resend.com/domains and use:
#   noreply@yourdomain.com
RESEND_FROM=onboarding@resend.dev

# ── Supabase (database + auth) ────────────────────────────────────────────────
# Get these from your Supabase project settings > API
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyxxxxxxxxxxxx
SUPABASE_SERVICE_ROLE_KEY=eyxxxxxxxxxxxx

# Supabase Transaction Pooler — port 6543 (works locally, no port blocking)
# Get this from: Supabase dashboard → Project Settings → Database → Connection string → Transaction pooler
# IMPORTANT: use port 6543, NOT 5432. Port 5432 is blocked by many ISPs.
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
DIRECT_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true

# Tenant ID — create one row in your Supabase Tenant table and paste the cuid here
DEFAULT_TENANT_ID=

# Admin email for form submission notifications
RESEND_ADMIN_EMAIL=
